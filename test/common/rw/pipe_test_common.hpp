/**
 * @file pipe_test_common.hpp
 * @brief `Pipe` 测试聚合 helper 入口。 Aggregation helper entry for `Pipe` tests.
 * @details 测试项目：
 *          1. 聚合 `Pipe` 传输与模式矩阵 helper。
 *          2. 聚合 `Pipe::Stream` 阻塞提交语义 helper。
 *          Test items:
 *          1. Aggregate `Pipe` transport and mode-matrix helpers.
 *          2. Aggregate `Pipe::Stream` blocking submission helpers.
 */
#pragma once

#include "pipe_stream_test_common.hpp"
#include "pipe_zero_test_common.hpp"
